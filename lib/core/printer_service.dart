import 'dart:io';

class PrinterService {
  static Future<void> printTicket(File file) async {
    // BASE SIMPLIFICADA (Bluetooth real se agrega en nivel 6)
    // aquí dejamos preparado el hook profesional

    print("PRINTING FILE: ${file.path}");
  }
}